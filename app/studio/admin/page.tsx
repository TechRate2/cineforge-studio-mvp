import { ComingSoon } from '@/components/studio/ComingSoon';

export default function AdminPage() {
  return (
    <ComingSoon
      title="Admin panel"
      hint="Style preset library / prompt library / credit & rate-limit config / LLM routing. Wire /api/v1/admin/*."
    />
  );
}
